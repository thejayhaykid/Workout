import React from 'react';
import styles from './Nav.module.scss';

const Nav = () => {
    return (<div className={styles.Nav}>
        <div className={styles.item}>Navigation Bar</div>
    </div>)
};

export default Nav;
